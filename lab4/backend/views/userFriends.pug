extends layout

block content
    h1 Friends
    if friends && friends.length
        each user in friends
            a.card-link(href='/users/' + user.id)
                h2= user.name + ' ' + user.second_name